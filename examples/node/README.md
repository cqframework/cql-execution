This mini-project demonstrates using `cql-execution` from regular Node.js JavaScript.
It depends on the repository's local `cql-execution` package.

From the repository root, install and build `cql-execution`:

```sh
npm install
```

Then install and run the example:

```sh
cd examples/node
npm install
npm start
```

Run `npm test` to execute the example's automated test.
